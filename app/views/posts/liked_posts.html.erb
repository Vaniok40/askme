<div class="my-posts-page">
  <div class="my-posts-header">
    <div>
      <h2 class="my-posts-title"><i class="fa fa-heart"></i> Postări apreciate</h2>
      <p class="my-posts-subtitle"><%= @posts.total_count %> <%= @posts.total_count == 1 ? 'postare' : 'postări' %> apreciate</p>
    </div>
  </div>

  <% if @posts.total_count == 0 %>
    <div class="my-posts-empty">
      <i class="fa fa-heart-o"></i>
      <p>Nu ai apreciat nicio postare încă.</p>
    </div>
  <% else %>
    <div class="my-posts-grid">
      <% @posts.each do |post| %>
        <div class="my-post-card">
          <div class="my-post-card-body" data-post-id="<%= post.id %>">
            <div class="my-post-meta-top">
              <div class="my-post-liked-author">
                <span class="my-post-author-name">
                  <i class="fa fa-user-o"></i>
                  <%= link_to post.user.name, user_path(post.user), class: 'my-post-author-link' %>
                </span>
              </div>
              <div class="my-post-stats">
                <span class="post-card-liked"><i class="fa fa-heart"></i> <%= post.likes.size %></span>
                <span><i class="fa fa-comment-o"></i> <%= post.comments.size %></span>
              </div>
            </div>
            <h3 class="my-post-title"><%= post.title %></h3>
            <p class="my-post-excerpt"><%= truncate(post.body, length: 160) %></p>
            <% if post.tags.any? %>
              <div class="my-post-tags">
                <% post.tags.each do |tag| %>
                  <span class="post-tag">#<%= tag.name %></span>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
    <%= paginate @posts %>
  <% end %>
</div>

<div class="side-panel" id="sidePanel">
  <button class="side-panel-close" id="sidePanelClose" title="Închide"><i class="fa fa-times"></i></button>
  <div class="side-panel-content" id="sidePanelContent">
    <div class="side-panel-loading"><i class="fa fa-spinner fa-spin"></i></div>
  </div>
</div>
<div class="side-panel-overlay" id="sidePanelOverlay"></div>
