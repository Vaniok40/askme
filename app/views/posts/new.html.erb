<div class="form-page">
  <h2><i class="fa fa-pencil"></i> New Post</h2>

  <%= form_with model: @post, url: posts_path, local: true, class: 'post-form' do |f| %>
    <% if @post.errors.any? %>
      <div class="flash-alert">
        <% @post.errors.full_messages.each do |msg| %>
          <div><%= msg %></div>
        <% end %>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title, 'Title' %>
      <%= f.text_field :title, class: 'form-control', placeholder: 'Give your post a title…' %>
    </div>

    <div class="form-group">
      <%= f.label :body, 'Content' %>
      <%= f.text_area :body, class: 'form-control', rows: 8, placeholder: 'Write your post… use #hashtags to tag topics' %>
    </div>

    <div class="form-actions">
      <%= f.submit 'Publish', class: 'btn btn-primary' %>
      <%= link_to 'Cancel', feed_path, class: 'btn btn-secondary' %>
    </div>
  <% end %>
</div>
